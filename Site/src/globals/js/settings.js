/**
 * Settings.
 * @exports RhComponents.settings
 * @type Object
 * @property {string} [prefix=rh]
 *   Brand prefix. Should be in sync with `$prefix` Sass variable in ./src/globals/scss/_vars.scss.
 */
const settings = {
    prefix: 'rh',
};
export default settings;